#include<stdio.h>
int main()
{
    int HS, MS, HE, ME, i, j, temp, timem, timeh;

    scanf("%d %d %d %d", &HS, &MS, &HE, &ME);

    if(HS == HE && MS == ME){
        printf("O JOGO DUROU 24 HORA(S) E 0 MINUTO(S)\n");
    }

    else{
        temp1 = SH;
        while(SH != EH){
            while(SM != EM){
                for(i= SM; i <= 60; i++){
                    timem++;
                    if()
                }
            }

            }
        }
    }

    return 0;
}
